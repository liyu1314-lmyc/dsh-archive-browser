# Changelog

本插件的所有重要改动都会记在这里。

## 0.2.3 — 2026-09-12

### 新增：「查看内容」按角色可视化

- 每条消息变成带**左侧色条**的卡片，三种"声音"一眼可分：
  **▎你（蓝）** · **▎助手（绿）** · **▎工具（灰）**，顶部有颜色图例
- **工具行**改成紧凑样式（灰底 + 等宽字），且**不再只有工具名** —— 现在带上"它干了什么"：
  - `pwsh · Get-ChildItem -Force …`
  - `read · D:\...\DSH插件开发备忘.md`
  - `glob · **/plugin*.json`
- 工具名做成标签（`🔧 pwsh`）；**失败的调用**标成 `🔧 read（失败）`

### 修复 ⚠️ 助手回复此前一条都没显示（pre-existing bug）

- `assistant/message` 记录的内容在 **`data.message.content`**，而代码读的是 `data.content`
  → `contentToText()` 永远返回空 → **转录里助手消息全部被静默丢掉**
  （实测同一会话：修复前 你 30 / 助手 **0** / 工具 453；修复后 你 9 / 助手 52 / 工具 97）
- 现在统一由 `messageContent()` 处理不同记录类型的形状
  （`user/message` → `data.content`；`assistant/message`、`tool/result` → `data.message.content`）

> 附带效果：助手文本回来后转录更容易达到 20 万字符的上限，界面会显示「已截断」
> （截断标记本来就有，这里只是提醒：大会话看到的是前一部分）。

## 0.2.2 — 2026-09-12

### 更快：打开面板改成「轻量载入」

- **默认顺序改为「最近活动↓」** —— 最近用过的归档会话排在最上面（顺序按钮仍可切换，选择会记住）
- 打开面板**不再解压任何转录**：每行的标题 / 首条消息 / 时间改从 DSH 自己的**投影缓存**
  （`<DSH_HOME>/storages/session_projcache/`）读取，外加一次 `stat`
  - 实测（9 个归档会话）：**4ms**；优化前 **333ms** —— 约 **80 倍**
  - 只有**没有**投影缓存的会话（本机从未打开过的）才回退到解压**它自己**，代价可控且罕见
- **消息数改为"点开才补"**：投影缓存里的计数与转录**口径不同**
  （`titleInput.count` 是送标题生成的提问数、`turnOutline.turns` 是**回合数**，
  而转录里数的是 `assistant/message` **条数** —— 实测同一会话 9 vs 135）。
  所以轻量列表**不显示计数**，避免悄悄换口径；点开该会话后，计数会补回那一行（也不再请求列表）
- 状态栏会说明「轻量载入：点开某个会话才读它的内容」

### 内部

- 顺序切换与状态栏文案改为共用一个函数（此前点排序会把「轻量载入」提示弄丢 —— 测试抓到）
- client_harness：断言改为「默认最近优先」，并新增「点开后补全计数且不多请求列表」的断言

## 0.2.1 — 2026-09-11

### 新增：列表「顺序」切换按钮

- 面板顶部多了一个「**顺序**」按钮，点一下换一种排法，循环切换：
  **归档顺序 → 最近活动↓ → 最近活动↑ → 标题**
- 选择会**记住**（浏览器 `localStorage`），下次打开还是它
- 排序在**客户端**完成：host 返回的归档顺序不变，每行自带的
  `lastActivity` / `createdAt` / `title` 足够重排，**不会因此多读一次归档**
- 状态栏会显示当前用的是哪种顺序（含一句大白话说明）

### 内部

- 离线测试的假 DOM 修正：`textContent = ''` 现在会**真的清空子节点**（真实浏览器的行为）。
  之前假 DOM 只是赋值、不清 children，于是列表重渲染时节点在测试里累积、浏览器里却正常
  —— 新加的排序测试第一次跑就把它暴露出来了。
- client_harness 新增 8 条断言（默认顺序、四种模式循环、状态栏文案、不重复请求）

## 0.2.0 — 2026-09-11

### 新增：「影响面」只读体检

每个归档会话多了一个「**影响面**」按钮，列出这段对话在你的电脑上留下过什么：

- **产出 / 修改的文件**：路径、**新建还是修改**、**现在还在不在**、大小、最后修改时间、被改过几次
- **执行过的命令**：按 **安装依赖 / 克隆仓库 / 下载 / 其它** 分类高亮，附命令自带的描述与失败标记
- **派生出去的会话**：**小助手（子代理，界面不可见）** 与 **分支（fork，界面可见）** 分开标注
- 每一步都有**大白话解释**，按每条的实际状态生成（不是套话）
- 新增 host 路由方法 `impact`

**只读**：不修改任何数据；文件只做"还在不在 / 多大 / 什么时候改的"三项检查，不读内容。

> 依赖**不会**被自动卸载 —— 面板只给清单，是否清理由你决定（一个软件包可能别处正在用）。

### 修复

- **「小助手」与「fork 分支」不再混为一谈**：`parentSession` 被两者共用，之前把 fork（界面里**看得见**
  的正常会话）也标成"界面里看不见的小助手"，是错的。现在按头部 `origin` 区分。
- **信标不再硬编码作者机器路径**：默认落系统临时目录（`%TEMP%\dsh-archive-browser.log`），
  可用 `ARCHIVE_BROWSER_BEACON` 指定路径或设成 `off` 完全关闭。
  之前写死在作者工作区里，在别人机器上会静默失效。

### 内部

- 命令分类在匹配前**剥掉引号内字符串** —— 否则 `"=== test outbound (curl/...)"` 这类横幅会让整条命令
  被误判成"下载"（实测把误判从 16 条降到 10 条）
- 截断标记不再把"重复修改同一文件"误报成结果被截断
- 面板加**热重载自保**：`buildPanel()` 先清掉遗留面板，避免 client 半热重载后叠出第二个面板
- 离线自测新增 18 条断言（影响面请求/渲染、命令分类、大白话层、fork 与 subagent 区分）

## 0.1.0 — 2026-09-11

- 首个版本：**查看内容** / **恢复到侧边栏** / **引入当前对话** / **岔出继续对话**
- host + client 双半结构；归档面板用原生 DOM 实现（规避 slot 条目"退位"问题）
